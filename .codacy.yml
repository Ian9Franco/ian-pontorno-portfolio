---
engines:
  eslint:
    enabled: true
  duplication:
    enabled: true
    config:
      min_length: 50

exclude_paths:
  - node_modules/**
  - .next/**
  - out/**
  - build/**
  - pnpm-lock.yaml
  - package-lock.json
  - "*.tsbuildinfo"
  - next-env.d.ts
  - public/**
